/*
Multi line comment

With lots of lines
*


*/

int main(void) {
	/*one line comment */
	int bo = 2^2^2; /*inline comment */
	int zo = /*mid-line comment8*/ bo;

	bo = 0x10;
	bo = 16;
	bo = 020;

	array[bo];
	array[bo[zo] + the[clown]];
	array[/*with comment*/ 020];
	array[/*this should just be "array[]"*/];
	return 0;
}