/*
Multi line comment

With lots of lines
*


*/

int main(void) {
	/*one line comment */
	int bo = 2^2^2; /*inline comment */
	int zo = /*mid-line comment*/ bo; /* and again */

	bo = 0x10;
	bo = 16;
	bo = 020;

	array[];
	array[bo];
	array[bo[zo] + the[clown]];
	array[/*with comment*/ 020 /* again */];
	array[/*this should just be "array[]"*/];
	array[/*so should this*/ /*dfdkj*/];
	array[	];
	array[
			];
	return 0;
}
